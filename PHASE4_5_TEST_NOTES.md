# 4·5차 브라우저 검증 기록

- 2026-08-17: `/calculator/tax/comprehensive-income-tax`를 데스크톱 브라우저에서 확인했다.
- 제목, 브레드크럼, 수입·경비·공제 입력 영역, 계산/초기화 버튼, 결과 카드, 계산 과정, FAQ 및 실제 관련 도구 링크가 렌더링되었다.
- 기본 예시값(수입 60,000,000원, 필요경비 20,000,000원, 소득공제 5,000,000원)의 결과는 과세표준 35,000,000원, 총 예상세액 4,851,000원으로 표시되었다.
- 화면 기준 가로 넘침은 관찰되지 않았다.

입력 필드에 테스트 금액을 넣고 계산 버튼을 조작하는 브라우저 자동화에서는 화면의 문자열 값은 변경되었지만 결과 카드가 즉시 갱신된 것으로 관측되지 않았다. 콘솔 오류는 없었으며, 공통 쉼표 숫자 입력 컴포넌트와 기존 적용 상태 패턴을 사용하는 화면이므로 자동화 이벤트 전달 특성 여부를 분리 검증할 필요가 있다. 순수 계산 함수의 대표값·경계값은 자동 테스트로 검증한다.

- `/calculator/date/d-day`는 제목, 날짜 입력, 계산/초기화 버튼, 결과 카드, FAQ 및 실제 관련 도구 링크를 정상 렌더링했다. 기본 예시값은 D-9 및 9일로 표시되었다.
- 자동화 환경의 날짜 입력 주입은 브라우저 로캘 형식과 결합되어 `2026-01-01`이 비정상 연도로 보이는 제약이 관측되었다. 이후 표준 DOM 이벤트로 날짜 변경을 검증한다.

- 표준 DOM `input`·`change` 이벤트로 목표일을 기준일과 같게 변경한 뒤 계산하기를 실행하여 결과가 `D-Day`, 0일, 오늘로 정상 갱신되는 것을 확인했다.

모바일 390×844 뷰포트에서 종합소득세와 D-Day 계산기 캡처를 점검했다. 두 화면 모두 메뉴가 모바일 아이콘으로 전환되었고, 제목·브레드크럼·입력폼·버튼·결과 영역이 컨테이너 폭 안에 표시되었으며 가로 넘침은 없었다. 자동 측정값에서도 두 페이지의 viewport, document, body 폭은 모두 390px로 일치했다.

단위 변환기 모바일 검증 과정에서 카탈로그가 생성하는 `/units/length/unit-convert` 경로에 단위 도구 라우트가 없어 404가 표시되는 기존 연결 누락을 발견했다. 기존 짧은 URL이나 계산 로직을 바꾸지 않고, 해당 카탈로그 경로를 `ConverterToolRoute`로 연결하는 라우트를 추가했다.

라우트 보완 후 `/units/length/unit-convert`는 모바일 390×844 화면에서 정상 렌더링되었다. 길이·무게·온도·넓이·부피 탭과 변환 입력·결과 영역이 모두 한 화면 폭 안에 표시되었고, 자동 측정에서도 가로 넘침이 없었다.
