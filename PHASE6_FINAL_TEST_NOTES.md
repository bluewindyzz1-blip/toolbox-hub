# 도구상자 최종 점검 기록

## 자동 검증

| 항목 | 결과 | 근거 |
|---|---:|---|
| 단위·계산기·카탈로그·파일 유틸리티 테스트 | 통과 | `pnpm test`: 5개 테스트 파일, 37개 테스트 통과 |
| TypeScript 검사 | 통과 | `pnpm check` 완료 |
| 프로덕션 빌드 | 통과 | `pnpm build` 완료 |

## 브라우저 기반 기능 검증

| 경로 | 확인 내용 | 결과 |
|---|---|---|
| `/convert/pdf-edit/pdf-merge` | SEO 제목, PDF 합치기 초기 모드, 2개 로컬 PDF 선택, 실제 병합 결과 생성 | 통과 |
| `/convert/document/csv-to-excel` | SEO 제목, CSV → Excel 초기 모드, 로컬 CSV 선택, 3행 XLSX 결과 생성 | 통과 |
| `/sitemap.xml` | 신규 PDF·문서 도구 URL 포함 | 통과 |

## 모바일 레이아웃 검증

390×844px 에뮬레이션에서 홈, PDF 합치기, CSV → Excel, 에너지 단위 변환을 점검했습니다. 모든 대상에서 `documentWidth`와 `bodyWidth`가 390px로 같았고, 가로 넘침은 감지되지 않았습니다. `mobile-pdf-merge.png`를 시각 확인한 결과, 모바일 메뉴·제목·파일 보안 안내·2열 작업 탭이 화면 너비 안에서 읽을 수 있게 배치되었습니다.

## 확인된 구현 원칙

현재 파일 도구는 브라우저 메모리 안에서만 처리하며, 서버 업로드·영구 저장·공개 다운로드 URL 생성을 하지 않습니다. PDF → Word는 원본 구조 보존을 보장할 수 있는 서버 변환 엔진 연결 전까지 비공개로 표시됩니다. 광고와 분석 스크립트는 실제 배포 환경변수가 모두 설정된 경우에만 로드됩니다.
