# 카큘레이트 PDF 기능 확장 보고서

## 이번 작업

기존 정적·브라우저 처리 구조를 유지하면서 `pdf-lib`를 추가하고 PDF 작업 화면을 확장했습니다. 파일은 서버로 전송하지 않으며, 현재 기기의 브라우저 메모리에서만 처리됩니다.

추가 또는 연결된 기능은 PDF 합치기, PDF 페이지별 분할, 페이지 추출, 페이지 삭제, 페이지 순서 변경, 페이지 회전, 텍스트 워터마크, 페이지 번호 삽입, PDF 메타데이터 편집, PDF→Excel용 CSV, PDF→한글 호환 HTML입니다. 기존 PDF→Word 호환 문서, PDF→이미지, 이미지→PDF 기능도 유지했습니다.

## 신규 URL

- `/convert/pdf/pdf-to-excel`
- `/convert/pdf/pdf-to-hwp`
- `/convert/pdf-edit/pdf-merge`
- `/convert/pdf-edit/pdf-split`
- `/convert/pdf-edit/pdf-page-edit`
- `/convert/pdf-edit/pdf-watermark`
- `/convert/pdf-edit/pdf-page-numbers`

기존 `/convert/pdf/pdf-convert` URL은 유지되며 화면 안에서 Word·Excel·한글 호환 문서·이미지·이미지→PDF 작업을 선택할 수 있습니다. 기존 계산기와 광고 설정은 변경하지 않았습니다.

## Word·Excel·한글 파일 변환의 정확한 범위

### Word

텍스트 레이어가 있는 PDF에서 텍스트를 추출해 Word 호환 `.doc` HTML 문서로 저장합니다. 표, 이미지, 글꼴, 원본 배치는 완전히 복원되지 않을 수 있습니다. 스캔 PDF는 OCR이 없어 텍스트가 추출되지 않습니다.

진짜 레이아웃 보존형 `.docx`를 무료로 만들려면 로컬 PC에서 `pdf2docx` 같은 오픈소스 Python 도구를 실행하는 방법이 있습니다. 다만 해당 방식은 서버 설치·처리 환경이 필요하고, 사이트의 정적 브라우저 처리와는 별도 운영 방식입니다. `pdf2docx` 프로젝트는 현재 적극적으로 유지보수되지 않는다는 안내가 있으므로 운영 핵심 기능으로 바로 의존하지 않는 것이 안전합니다.

### Excel

PDF의 텍스트 레이어를 페이지 단위로 추출해 `페이지,텍스트` 형식의 UTF-8 BOM CSV로 저장합니다. Excel에서 열 수 있지만 표의 행·열 구조와 수식은 복원하지 않습니다. 표 구조를 정확하게 복원하려면 전용 PDF 표 분석 엔진이나 OCR이 필요합니다.

### 한글

전용 HWP 바이너리 파일을 브라우저에서 직접 생성하지 않고, 한글에서 열 수 있는 HTML 문서로 저장합니다. 사용자는 한글에서 HTML을 연 다음 한글 문서 형식으로 다시 저장할 수 있습니다. 이는 무료·무서버 방식의 현실적인 대안이며, 원본 PDF의 복잡한 배치는 유지되지 않을 수 있습니다.

## 검증 결과

- `pnpm check`: 통과
- `pnpm build`: 통과
- `pnpm test`: 5개 테스트 파일, 29개 테스트 통과
- 로컬 Production 서버에서 `/convert/pdf-edit/pdf-merge` 직접 URL 렌더링 확인
- 로컬 Production 서버에서 `/convert/pdf/pdf-to-excel` 직접 URL 렌더링 확인
- 각 페이지의 SEO title과 브라우저 전용 처리 안내 노출 확인

빌드에는 기존 분석 환경변수 미설정 경고와 PDF 처리 청크 크기 경고가 남아 있습니다. 이는 빌드 실패가 아닌 경고이며, 이번 작업에서는 기존 사이트 설정을 보존하기 위해 변경하지 않았습니다.

## 아직 추가하지 않은 기능

OCR, 정확한 PDF→DOCX·XLSX·PPTX 변환, 손상 PDF 복구, 전자서명, AI 요약·번역, 실제 보안 정보 영구 삭제(redaction)는 정적 브라우저 처리만으로 품질과 보안을 보장하기 어렵습니다. 향후 추가하려면 WASM 엔진 또는 별도 처리 서버·API·저장·삭제 정책이 필요합니다.

## 무료 운영 권장안

현재 단계에서는 브라우저에서 바로 처리 가능한 PDF 작업을 먼저 제공하고, Word·Excel·한글은 각각 호환 형식임을 명확히 고지하는 방식을 권장합니다. 사용자에게 원본 파일을 외부로 보내지 않는 장점을 제공하면서도, DOCX·XLSX·HWP 원본 형식의 완벽한 변환을 약속하지 않는 것이 정확합니다.
