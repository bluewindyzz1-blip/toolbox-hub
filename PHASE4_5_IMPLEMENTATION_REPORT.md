# 도구상자 4·5차 계산기 구현 완료 보고서

**작성일:** 2026-08-17 (KST)  
**작성자:** Manus AI  
**대상:** 기존 `toolbox-hub` 프로젝트 확장 구현

## 1. 완료 요약

기존 React·TypeScript 카탈로그 구조와 공통 계산기 UI를 보존한 채, 4차 세금·생활·사회보험과 5차 날짜·시간·단위·일상생활 요구사항을 구현했습니다. 신규 기능은 `shared/toolbox.ts`의 순수 계산 함수, 데이터 기반 카탈로그, 기존 안전 라우터 및 공통 화면 프레임에 연결했습니다. 기존 계산기·URL·디자인을 삭제하거나 재작성하지 않았습니다.

요청 목록 중 이미 실제 기능으로 존재하던 부가세, 실업급여, 근무시간, 시급, 단위 변환, 평수 계산기는 같은 기능을 중복 생성하지 않았습니다. 기존 URL과 계산 로직은 유지하고, 검색 별칭·내부 관련 링크·단위 범위 또는 모바일 경로를 보강했습니다. 이 방식으로 4차와 5차 모두 요청된 사용자 접근 항목 10개를 충족합니다.

| 차수 | 신규 구현 | 기존 유지·보강 | 합계 |
| --- | ---: | ---: | ---: |
| 4차 | 8개 | 부가세, 실업급여 | 10개 |
| 5차 | 6개 | 근무시간, 시간당 급여, 단위 변환, 평수 | 10개 |

## 2. 4차 구현 목록

| 계산기 | URL | 카테고리 | 주요 입력 및 구현 로직 |
| --- | --- | --- | --- |
| 종합소득세 | `/calculator/tax/comprehensive-income-tax` | 세금 > 소득·신고 | 수입, 필요경비, 소득·세액공제, 기납부세액을 입력해 과세표준·누진세액·지방소득세·추가납부/환급을 계산합니다. |
| 양도소득세 | `/calculator/tax/capital-gains-tax` | 세금 > 자산·부동산 세금 | 양도·취득가액, 필요경비, 취득·양도일을 입력해 보유기간·양도차익·기본공제·간이 세액을 계산합니다. |
| 증여세 | `/calculator/tax/gift-tax` | 세금 > 자산·부동산 세금 | 재산가액, 인수채무, 관계, 미성년 여부, 이전 증여액으로 공제·과세표준·누진세액을 계산합니다. |
| 상속세 | `/calculator/tax/inheritance-tax` | 세금 > 자산·부동산 세금 | 상속재산, 채무·비용, 공제, 사전증여액으로 과세가액·세액을 계산합니다. |
| 연말정산 환급금 | `/calculator/tax/year-end-tax-refund` | 세금 > 연말정산 | 총급여, 비과세, 부양가족, 공제, 기납부 소득·지방소득세로 환급/추가납부를 계산합니다. |
| 부가세 | `/calculator/tax/vat-calculator` 및 `/vat` | 세금 계산기 | 기존 공급가액·부가세 포함금액 양방향 계산을 보존하고 종합소득·연말정산 관련 링크와 검색 별칭을 보강했습니다. |
| 국민연금 예상수령액 | `/calculator/lifestyle/national-pension` | 생활 > 사회보험 | 기준소득월액, 현재·추가 가입기간을 입력해 10년 요건 및 월·연 예상액을 간이 추정합니다. |
| 건강보험료 | `/calculator/lifestyle/health-insurance` | 생활 > 사회보험 | 직장·지역가입자 유형, 소득월액, 재산점수로 건강·장기요양보험료를 계산합니다. |
| 실업급여 | `/calculator/salary/unemployment-benefit` | 급여·직장인 > 퇴직·고용 | 기존 실제 계산 로직과 URL을 보존하고 최저임금 계산기와 내부 링크를 연결했습니다. |
| 최저임금 월급 | `/calculator/salary/minimum-wage` | 급여·직장인 > 근로·수당 | 시급, 일 근로시간, 주 근무일수, 주휴 포함 여부로 주급·주휴수당·월 환산액·최저시급 충족 여부를 계산합니다. |

세금 계산기는 복잡한 특례를 확정 계산처럼 표시하지 않도록 적용 범위를 결과 아래에 명시했습니다. 상속·증여세는 국세청이 안내하는 10%~50% 5단계 누진 구조와 관계별 증여공제의 간이 범위를 사용하며, 양도소득세는 비과세·중과·감면·장기보유특별공제를 제외한 일반 자산용 계산기임을 분명히 표시합니다.[1] [2] [3]

국민연금·건강보험·최저임금 결과에는 2026년 기준값을 적용하고, 실제 수급·부과·정산 결과와 차이날 수 있는 범위를 화면에 표시했습니다. 국민연금 사업장가입자의 2026년 7월 이후 기준소득월액 상·하한과 4.75% 분담 구조, 건강보험료율 및 장기요양보험료율, 최저임금 고시는 출처를 기준으로 반영했습니다.[4] [5] [6]

## 3. 5차 구현 목록

| 계산기 | URL | 카테고리 | 주요 입력 및 구현 로직 |
| --- | --- | --- | --- |
| 날짜 계산기 | `/calculator/date/date-calculator` | 날짜·시간 > 날짜 연산 | 기준일, 더하기/빼기, 연·월·일로 날짜를 연산하며 월말을 해당 월 마지막 날로 보정합니다. |
| D-Day | `/calculator/date/d-day` | 날짜·시간 > 날짜 연산 | 기준일·목표일로 D-Day, 남은/경과 일수와 주·일을 계산합니다. |
| 나이 계산기 | `/calculator/date/age` | 날짜·시간 > 나이 | 생년월일·기준일로 만 나이, 세는 나이, 연 나이, 다음 생일까지의 일수를 계산합니다. |
| 만 나이 | `/calculator/date/man-age` | 날짜·시간 > 나이 | 생년월일·기준일로 만 나이, 올해 생일 경과 여부, 다음 생일까지의 일수를 계산합니다. |
| 날짜 차이 | `/calculator/date/date-difference` | 날짜·시간 > 날짜 연산 | 시작·종료일 및 포함일 옵션으로 총일수, 주·일, 완전한 개월·잔여일을 계산합니다. |
| 근무시간 | `/calculator/salary/work-hours` | 급여·직장인 > 근로·수당 | 기존 자정 경과·휴게시간 검증 로직과 URL을 유지했습니다. 시간 계산기와 내부 링크를 추가했습니다. |
| 시간 계산기 | `/calculator/date/time-calculator` | 날짜·시간 > 날짜 연산 | 기준 시각, 더하기/빼기, 시간·분으로 결과 시각과 전날/당일/다음 날을 계산합니다. |
| 시간당 급여 | `/calculator/salary/hourly-wage` | 급여·직장인 > 근로·수당 | 기존 시급↔월급 양방향 계산 로직과 URL을 유지하고 ‘시간당 급여’ 검색 별칭 및 최저임금 내부 링크를 추가했습니다. |
| 단위 변환 | `/units/length/unit-convert` 및 `/unit` | 단위 변환 > 길이 | 기존 길이·무게·넓이·온도에 **부피**(mL, L, m³, 250mL 컵, US gal)를 추가했습니다. |
| 평수 | `/calculator/real-estate/pyeong` | 부동산 계산기 | 기존 ㎡↔평 양방향 계산과 `1평 = 3.305785㎡` 산식을 유지하고 검색 별칭을 보강했습니다. |

날짜 계산은 `YYYY-MM-DD` 달력일을 UTC 기준으로 정규화해 윤년, 2월 29일, 월말 보정과 날짜 역전 오류를 처리합니다. 날짜 차이는 기본 차이와 양 끝 날짜 포함 결과를 명확히 분리하고, D-Day는 목표일과 기준일이 같을 경우 `D-Day`, 과거일 경우 `D+n`으로 표시합니다.

## 4. 카탈로그·검색·홈페이지·내부 연결

신규 카테고리 `세금 > 소득·신고`, `세금 > 자산·부동산 세금`, `세금 > 연말정산`, `생활 > 사회보험`, `날짜·시간 계산기 > 날짜 연산/나이`를 기존 자기참조 카테고리 구조에 추가했습니다. 기존 URL 생성 규칙은 그대로 사용하므로 신규 계산기는 카테고리, 홈페이지의 인기 도구 영역, 검색 및 관련 도구 카드에 데이터로 연결됩니다.

| 기능 | 반영 방식 | 결과 |
| --- | --- | --- |
| 검색 | `searchKeywords`에 종소세·양도세·건보료·디데이·만나이·시간당 급여·부피 등 별칭을 등록 | 신규·보강 도구가 기존 검색 필터에 자동 포함 |
| 관련 도구 | `relatedToolIds`에 실제 활성 도구만 등록 | 종합소득세↔연봉/퍼센트/부가세, 양도세↔취득세/재산세/중개수수료, D-Day↔날짜/날짜차이/나이 등 연결 |
| 인기 도구 | 기존 `isPopular`, `sortOrder` 구조를 사용 | 연말정산 환급금과 D-Day가 기존 인기 도구를 삭제하지 않고 인기 후보에 추가 |
| 홈페이지 접근 | 카탈로그 기반 인기 카드·카테고리 목록 사용 | 별도 하드코딩 없이 노출 대상에 포함 |
| 단위 도구 URL | 카탈로그가 생성하는 3단계 단위 URL에 라우트를 추가 | `/units/length/unit-convert`가 실제 변환 화면으로 연결 |

## 5. 검증 결과

| 검증 항목 | 결과 | 세부 내용 |
| --- | --- | --- |
| 자동 테스트 | 통과 | Vitest 테스트 파일 5개, 총 35개 테스트 통과. 기존 1~3차 회귀와 신규 세금·사회보험·날짜·시간·부피 변환 대표값을 포함합니다. |
| 타입 검사 | 통과 | `pnpm check` 성공. |
| 프로덕션 빌드 | 통과 | 클라이언트, SSR, 서버 번들 생성 성공. |
| 정상·경계 입력 | 통과 | 0, 음수, 소수, 큰 수, 잘못된 날짜·시각, 누진세율 경계, 월말·윤년·자정 경과를 순수 함수 테스트에 포함했습니다. |
| 계산/초기화 | 통과 | D-Day의 목표일을 기준일과 같게 변경한 뒤 계산 결과가 `D-Day`, 0일, 오늘로 갱신되는 것을 실제 브라우저에서 확인했습니다. |
| 데스크톱 UI | 통과 | 종합소득세 화면에서 입력, 결과, 계산 과정, FAQ, 실제 관련 링크를 확인했습니다. |
| 모바일 UI | 통과 | 390×844 뷰포트에서 종합소득세, D-Day, 단위 변환의 `documentWidth`와 `bodyWidth`가 모두 390px으로 측정되어 가로 넘침이 없었습니다. 화면 캡처도 확인했습니다. |
| 카탈로그 URL | 통과 | 신규 세금·날짜 도구의 URL 생성과 단위 변환 카탈로그 URL 라우팅을 자동·브라우저 방식으로 확인했습니다. |

프로덕션 빌드는 기존 분석 스크립트 환경변수 미설정 경고와 대형 PDF 청크 경고를 표시했지만, 번들 생성 및 실행을 차단하지 않았습니다. 이 경고는 이번 계산기 확장의 기능 오류가 아니며 기존 성능·환경 설정 개선 과제로 남습니다.

## 6. 변경된 핵심 파일

| 파일 | 변경 내용 |
| --- | --- |
| `shared/toolbox.ts` | 세금·사회보험·최저임금·날짜·시간 계산 함수, 정책 상수, 부피 단위 정의 추가 |
| `shared/catalog.ts` | 카테고리·도구·SEO·검색 별칭·관련 링크·인기 설정 추가 |
| `client/src/pages/TaxSocialCalculators.tsx` | 4차 신규 8개 계산기의 독립 화면 구현 |
| `client/src/pages/DateTimeCalculators.tsx` | 5차 신규 6개 계산기의 독립 화면 구현 |
| `client/src/pages/CatalogPages.tsx` | 신규 `logicKey`의 안전 라우터 연결 |
| `client/src/pages/UnitConverter.tsx` | 부피 탭 및 소개 문구 보강 |
| `client/src/App.tsx` | 카탈로그 기반 단위 도구의 3단계 URL 라우팅 보완 |
| `server/toolbox.test.ts` | 신규 로직·경계값·URL·단위 변환 자동 테스트 추가 |

## 7. 운영 시 유의사항

세금, 사회보험, 최저임금, 실업급여는 제도 변경 가능성이 높은 영역입니다. 구현한 계산기는 화면의 범위·주의 안내대로 참고용 간이 계산기로 사용해야 하며, 실제 신고·납부·수급 신청 전에는 공식 서비스를 확인해야 합니다. 고용24도 모의계산 결과만으로 수급자격이 인정되지 않으며, 실제 근무일수·퇴사사유 등에 따라 달라질 수 있다고 안내합니다.[7]

정책 수치는 `POLICY_2026`과 기존 `PAYROLL_2026`에 분리해 두었으므로, 다음 연도 업데이트 시 해당 상수와 테스트 표본·화면 설명을 함께 점검해야 합니다. 특히 국민연금 예상수령액은 공단의 실제 가입이력·재평가율 등이 반영되지 않은 설명용 추정 결과임을 유지해야 합니다.

## 참고 자료

[1]: https://www.nts.go.kr/nts/cm/cntnts/cntntsView.do?mi=6529&cntntsId=7957 "국세청 — 상속세 세율"

[2]: https://www.nts.go.kr/nts/cm/cntnts/cntntsView.do?mi=6533&cntntsId=7960 "국세청 — 증여세 항목별 설명"

[3]: https://www.nts.go.kr/nts/cm/cntnts/cntntsView.do?mi=2312&cntntsId=7711 "국세청 — 양도소득세 세율"

[4]: https://www.nps.or.kr/eng/ntnlpnsplan/cntb/getOHAI0013M0.do "국민연금공단 — 보험료 및 기준소득월액"

[5]: https://edi.nhis.or.kr/portal/images/popup/20251204_pop01longdesc.html "국민건강보험공단 — 2026년도 보험료율 안내"

[6]: https://www.moel.go.kr/info/lawinfo/instruction/view.do?bbs_seq=20250800121 "고용노동부 — 2026년 적용 최저임금 고시"

[7]: https://m.work24.go.kr/cm/c/f/1100/selecSimulate.do?systId=SI00000001 "고용24 — 구직급여 지급액 모의계산"
