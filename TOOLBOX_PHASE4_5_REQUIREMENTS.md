# 도구상자 4·5차 계산기 확장 요구사항 명세서

**작성일:** 2026-08-17 (KST)  
**작성자:** Manus AI  
**문서 목적:** 3차까지 구축된 도구상자 프로젝트를 **새로 만들지 않고**, 기존 카탈로그·URL·디자인·공통 계산기 구조를 보존한 채 4차(세금·생활·사회보험)와 5차(날짜·시간·단위·일상생활) 기능을 구현하기 위한 제품 및 개발 기준을 정의한다.

## 1. 결론 및 추진 원칙

이번 요구사항은 단순히 20개의 독립 화면을 늘리는 작업이 아니다. 이미 프로젝트에는 데이터 기반 카탈로그, `logicKey` 기반의 안전한 계산기 라우팅, 검색 별칭, 관련 도구, 인기 도구, SEO 메타데이터 및 공통 계산기 화면이 존재한다. 따라서 신규 기능은 이 구조에 **추가 등록·연결**하는 방식으로 구현해야 한다. 기존 화면을 재작성하거나 URL을 이동시키는 방식은 허용하지 않는다.

현재 카탈로그에는 이미 **부가세 계산기, 실업급여 계산기, 근무시간 계산기, 시급 계산기, 단위 변환기, 평수 계산기**가 존재한다. 이 중 부가세·실업급여는 4차의 요청과, 근무시간·시급·단위 변환·평수는 5차의 요청과 겹친다. 그러므로 명세상 “20개 계산기”는 **20개 사용자 접근 항목**으로 관리하되, 이미 운영 중인 6개를 중복 생성하지 않는다. 기존 도구는 URL과 핵심 계산식을 그대로 유지하고, 필요한 정보·검색·관련 도구·표시 범위만 보강한다. 이 원칙이 기존 기능 보호와 중복 URL 방지라는 원요청을 동시에 충족한다.

> **핵심 결정:** 4차는 **신규 8개 + 기존 2개 유지·보강**, 5차는 **신규 6개 + 기존 4개 유지·보강**으로 정의한다. 신규 메타데이터를 넣는 것만으로는 충분하지 않으며, 각 `logicKey`는 실제 계산 화면과 안전한 라우팅에 연결되어야 공개 상태가 된다.

| 우선순위 | 반드시 지킬 원칙 | 구체적 의미 |
| --- | --- | --- |
| P0 | 기존 기능 비파괴 | 기존 계산 로직, URL, 디자인 토큰, 법적 안내, 카테고리, 검색 동작을 삭제·변경하지 않는다. |
| P0 | 공식 기준·버전 관리 | 세율·공제·보험료율·상하한·최저임금은 로직에 산재한 숫자로 넣지 않고, 출처·적용기간을 갖는 정책 데이터로 관리한다. |
| P0 | 실제 계산 가능 | 단순 설명 페이지가 아니라 유효성 검사, 계산, 계산 과정, 초기화, 오류 메시지를 제공한다. |
| P1 | 데이터 기반 탐색 | 카테고리, 검색 별칭, 인기 여부, 관련 도구, SEO 문구를 카탈로그 데이터로 관리한다. |
| P1 | 범위의 명확성 | 복잡한 세제·보험 제도는 반영 범위와 제외 조건을 결과 바로 옆에 표시한다. |
| P1 | 모바일 완결성 | 390px 폭에서도 입력, 날짜 선택, 결과표, FAQ, 관련 도구가 가로로 넘치지 않아야 한다. |

## 2. 현재 프로젝트 기준선과 영향 범위

프로젝트는 React·TypeScript 기반의 공개 계산기와 데이터베이스 기반 카탈로그를 분리해 두었다. 도구 레코드에는 제목, 설명, 카테고리, `slug`, `logicKey`, 입력 정의, 공식, FAQ, 관련 도구 ID, SEO, 공개 상태, 정렬 순서, 인기 여부 및 검색 별칭이 존재한다. URL은 카테고리 계보에서 생성되며, 기능 메타데이터를 등록한 뒤에도 화면 라우터에 연결하지 않으면 준비 중 화면이 표시된다. 따라서 이번 개발의 단위는 **정책 데이터 → 순수 계산 함수 → 화면 → 카탈로그 → 라우팅 → 테스트**다.

| 현재 자산 | 4·5차에서의 사용 방식 | 변경 금지 항목 |
| --- | --- | --- |
| `shared/toolbox.ts` 계산 함수 | 신규 함수와 정책 참조 함수를 추가한다. 날짜·단위·입력 정규화 공통 함수를 재사용한다. | 기존 함수의 입출력·산식·동작을 변경하지 않는다. |
| `shared/catalog.ts` 카탈로그 | 신규 카테고리/도구, `logicKey`, `searchKeywords`, `relatedToolIds`, SEO를 추가한다. | 기존 ID·slug·도구 레코드를 삭제하거나 다른 URL로 이동하지 않는다. |
| `CatalogPages.tsx` 안전 라우터 | 신규 슬러그를 전용 또는 묶음 화면 컴포넌트로 연결한다. | 기존 분기와 기존 경로를 제거하거나 재배치하지 않는다. |
| `ToolFrame`, `CalculatorActions`, `ToolKnowledge` | 신규 모든 페이지의 공통 UI·버튼·지식 영역으로 사용한다. | 신규 계산기를 위한 별도 전면 디자인 시스템을 만들지 않는다. |
| 카탈로그 검색·홈 인기 도구 | 별칭 및 `isPopular`, `sortOrder`만 데이터로 등록한다. | 검색 알고리즘과 기존 인기 도구를 임의 삭제하지 않는다. |

## 3. 범위 정리: 신규·기존 기능의 충돌 해소

요청된 명칭이 기존 도구와 겹치는 경우, 같은 계산기를 다른 URL로 한 번 더 만들지 않는다. 대신 사용자에게는 요청된 계산기 목록이 모두 검색되고 탐색되도록 하며, 기존 화면의 핵심 로직은 보호한다. 아래 표는 구현 범위를 판정하는 기준이다.

| 차수 | 요청 항목 | 판정 | 구현 요구사항 |
| --- | --- | --- | --- |
| 4차 | 종합소득세, 양도소득세, 증여세, 상속세, 연말정산 환급금 | 신규 | 세금 정책 데이터 기반의 독립 계산기 5개를 추가한다. |
| 4차 | 부가세 | 기존 활용·보강 | 기존 공급가액↔부가세 포함금액 양방향 계산은 그대로 둔다. 일반과세자용 **매출·매입세액 차감 추정 모드**를 별도 모드로 추가할 수 있으나, 기존 모드의 계산식·URL은 변경하지 않는다. |
| 4차 | 국민연금 예상수령액, 건강보험료, 최저임금 월급 | 신규 | 국민연금은 검증된 연금 산식 기반의 추정, 건강보험은 직장·지역가입자 모드, 최저임금은 시간·주·월 단위 계산으로 구현한다. |
| 4차 | 실업급여 | 기존 활용·보강 | 기존 URL과 계산 로직은 보존한다. 최신 정책 데이터의 출처·적용일 표기, 입력 한계와 수급자격 경고를 강화한다. |
| 5차 | 날짜, D-Day, 나이, 만 나이, 날짜 차이, 시간 계산기 | 신규 | 달력 기준의 순수 날짜 함수와 전용 화면 6개를 추가한다. |
| 5차 | 근무시간, 시간당 급여, 단위 변환, 평수 | 기존 활용·보강 | 기존 URL과 기본 산식을 보호한다. 단위 변환에는 부피 단위를 보강하고, 각 도구에 요청 키워드·관련 도구·모바일 검증을 추가한다. |

## 4. 4차 기능 요구사항: 세금·생활·사회보험

### 4.1 4차 카테고리·URL·탐색 설계

세금은 기존 `계산기 > 세금 계산기` 아래에서 탐색한다. 기존 부가세 계산기는 현재 카테고리에 직접 연결되어 있으므로 URL 보존을 위해 이동시키지 않는다. 신규 세금 도구만 하위 그룹에 등록하고, 세금 카테고리 페이지는 직접 연결 도구와 하위 그룹을 함께 노출한다. 사회보험 전용 기능은 기존 급여 계산기와 구분할 수 있도록 `생활 계산기 > 사회보험`에 배치한다. 최저임금은 이미 존재하는 급여·직장인 맥락과 검색 의도가 강하므로 `급여·직장인 계산기 > 근로·수당`에 둔다.

| 기능 | 구분 | 카테고리 배치 | 정규 URL | 핵심 검색 별칭 | 관련 도구 우선순위 |
| --- | --- | --- | --- | --- | --- |
| 종합소득세 계산기 | 신규 | 세금 > 소득·신고 | `/calculator/tax/comprehensive-income-tax` | 종합소득세, 종소세, 종합소득, 프리랜서 세금 | 연봉 실수령액, 퍼센트, 부가세 |
| 양도소득세 계산기 | 신규 | 세금 > 자산·부동산 세금 | `/calculator/tax/capital-gains-tax` | 양도소득세, 양도세, 부동산 양도 | 취득세, 재산세, 부동산 중개수수료 |
| 증여세 계산기 | 신규 | 세금 > 자산·부동산 세금 | `/calculator/tax/gift-tax` | 증여세, 가족 증여, 자녀 증여 | 상속세, 취득세 |
| 상속세 계산기 | 신규 | 세금 > 자산·부동산 세금 | `/calculator/tax/inheritance-tax` | 상속세, 상속 재산, 상속 공제 | 증여세, 취득세 |
| 연말정산 환급금 계산기 | 신규 | 세금 > 연말정산 | `/calculator/tax/year-end-tax-refund` | 연말정산, 환급금, 추가납부, 소득공제 | 연봉 실수령액, 월급 실수령액, 4대보험 |
| 부가세 계산기 | 기존 유지·보강 | 세금 계산기 직접 연결 유지 | `/calculator/tax/vat-calculator` 및 기존 짧은 URL | 부가세, VAT, 공급가액, 매입세액 | 종합소득세, 퍼센트 |
| 국민연금 예상수령액 계산기 | 신규 | 생활 > 사회보험 | `/calculator/lifestyle/national-pension` | 국민연금, 노령연금, 연금 예상액 | 4대보험, 연봉 실수령액 |
| 건강보험료 계산기 | 신규 | 생활 > 사회보험 | `/calculator/lifestyle/health-insurance` | 건강보험료, 건보료, 지역가입자, 장기요양 | 4대보험, 연봉 실수령액 |
| 실업급여 계산기 | 기존 유지·보강 | 급여·직장인 > 퇴직·고용 유지 | `/calculator/salary/unemployment-benefit` | 실업급여, 구직급여, 고용보험 | 최저임금 월급, 연봉 실수령액, 퇴직금 |
| 최저임금 월급 계산기 | 신규 | 급여·직장인 > 근로·수당 | `/calculator/salary/minimum-wage` | 최저임금, 최저시급, 최저월급, 주휴수당 | 시급 계산기, 주휴수당, 근무시간 |

**URL 주의사항:** 요청 예시의 `/calculator/life/*`는 현재 프로젝트의 실제 상위 슬러그인 `lifestyle`과 다르다. 기존 URL 규칙을 우선하여 신규 사회보험 도구는 `/calculator/lifestyle/*`를 사용한다. 또한 기존 부가세·실업급여의 URL은 변경하지 않는다.

### 4.2 세금 계산기 공통 정책 요구사항

세금 도구는 실제 신고 프로그램을 복제하는 것이 아니라, 사용자가 입력한 범위 안에서 과세표준과 예상 세액을 이해하도록 돕는 **참고용 간이계산기**다. 모든 결과에는 `정책 기준일`, `정책 버전`, `공식 출처`, `포함된 항목`, `제외된 특례`, `실제 신고 전 확인 안내`를 같은 위치에 표기해야 한다.

국세청은 상속·증여세에 10%~50%의 5단계 초과누진 구조를 안내하며, 증여세는 관계별 10년 합산 공제한도와 이전 증여분 합산 조건을 별도로 설명한다.[1] [2] 양도소득세는 보유기간, 자산 종류, 주택 수, 지역 및 적용 시점에 따라 세율이 달라지는 구조이므로, “양도차익 × 단일 세율” 방식으로 실제 세액처럼 표시해서는 안 된다.[3] 연말정산도 공제증명자료·부양가족 등 입력 조건에 따라 예상세액이 산출되는 서비스이므로, 단순 월급만으로 확정 환급액을 약속할 수 없다.[4]

| 공통 항목 | 필수 요구사항 |
| --- | --- |
| 정책 데이터 | `effectiveFrom`, `effectiveTo`, `sourceUrl`, `sourceTitle`, `lastVerifiedAt`, 세율표, 공제표, 상·하한을 포함한 버전 객체로 관리한다. 하드코딩된 숫자를 화면에 직접 넣지 않는다. |
| 적용 시점 | 사용자가 귀속·양도·증여·상속 또는 신고 기준 연도를 선택하거나, 기본값을 현재 적용 가능한 정책 버전으로 설정한다. 유효기간 밖 값은 계산하지 않고 “기준 확인 필요” 상태로 안내한다. |
| 계산 과정 | 입력값 → 공제/필요경비 → 과세표준·기준금액 → 세율 구간 → 국세/지방소득세·공제 → 최종 추정액을 항목별로 표시한다. |
| 결과 상태 | 정상 계산, 추가 정보 필요, 범위 제외, 입력 오류를 시각적으로 구분한다. 범위 제외 조건에서는 0원으로 단정하지 않는다. |
| 개인정보 | 주민등록번호, 상세 주소, 가족 실명, 계좌·홈택스 인증정보를 요구하거나 저장하지 않는다. |
| 업데이트 | 정책 데이터 갱신 시 자동화 테스트의 대표 세율구간·경계값을 함께 갱신하고, 화면에 갱신일을 노출한다. |

### 4.3 4차 계산기별 상세 요구사항

| 계산기 | 최소 입력값 | 계산 결과·과정 | 명시적 범위 및 제외 조건 |
| --- | --- | --- | --- |
| 종합소득세 | 수입금액, 필요경비, 소득공제 합계, 세액공제 합계, 기납부세액, 귀속연도 | 종합소득금액, 과세표준, 누진세율 산출세액, 세액공제 후 소득세, 지방소득세, 납부/환급 추정액 | 사업·근로·연금·기타소득의 합산 방식은 선택된 간이범위에 한정한다. 분리과세, 결손금, 특별공제, 성실신고·가산세 등은 제외를 명시한다. |
| 양도소득세 | 자산 유형, 양도가액, 취득가액, 자본적 지출·양도비, 취득일, 양도일, 기본공제 적용 여부, 기준연도 | 양도차익, 필요경비, 보유기간, 장기보유특별공제(지원 자산만), 과세표준, 세율/누진공제, 양도소득세, 지방소득세 | 기본형은 일반 부동산·권리 등 지원 자산을 명시한다. 1세대 1주택 비과세, 다주택 중과, 조정대상지역, 비사업용 토지, 분양권, 주식, 감면은 별도 모드가 검증될 때까지 범위 제외로 표기한다. |
| 증여세 | 증여재산가액, 승계 채무, 증여자와 관계, 수증자 미성년 여부, 10년 내 동일인 증여액·기공제액, 기준연도 | 순증여재산가액, 합산 과세가액, 관계별 공제, 과세표준, 누진세액, 신고세액공제 선택 시 예상 납부세액 | 특례세율, 세대생략 할증, 창업·가업승계, 평가방법 차이, 비과세·불산입 재산은 별도 확인이 필요하다고 표시한다. |
| 상속세 | 상속재산, 비과세/불산입 재산, 채무·공과·장례비, 배우자공제 입력값, 인적/일괄공제 선택, 사전증여 합산액, 기준연도 | 순상속재산가액, 공제합계, 과세표준, 누진세액, 신고세액공제 선택 시 예상 납부세액 | 배우자공제의 법정 요건·한도, 금융재산 공제, 동거주택 공제, 가업상속, 세대생략, 재산평가·배분은 단순입력 범위 밖임을 표시한다. |
| 연말정산 환급금 | 총급여, 비과세 급여, 인적공제 인원, 주요 소득공제/세액공제 합계, 기납부 소득세·지방소득세, 기준연도 | 근로소득금액, 과세표준, 산출세액, 공제 후 결정세액, 기납부세액 대비 환급 또는 추가납부 예상액 | 사용자가 보유한 원천징수영수증 및 간소화자료를 입력하는 보조도구다. 실제 공제 요건·한도, 회사별 원천징수, 가족 중복공제는 국세청 서비스에서 확인하도록 한다. |
| 부가세 | **기존 모드:** 공급가액 또는 부가세 포함금액. **보강 모드:** 과세 매출 공급가액, 공제가능 매입세액, 기타 공제·가산 항목 | 기존 모드의 공급가액·VAT·합계. 보강 모드의 매출세액, 매입세액, 추정 납부/환급액 | 기존 양방향 환산은 변경 금지다. 보강 모드는 일반과세자 간이 추정으로 한정하며, 간이과세·의제매입·영세율·매입세액 불공제·가산세는 확정 계산 대상이 아니다. |
| 국민연금 예상수령액 | 생년월일, 기준연금수급개시 연령, 현재 가입기간, 예상 추가 가입기간, 기준소득월액 이력/평균, 기준연도 | 가입기간 충족 여부, 총 예상 가입월수, 적용된 기준소득월액, 노령연금 월 예상액, 조기/연기 수령 조정이 지원될 경우 조정 결과 | 단순 비율 곱셈은 금지한다. 검증된 법정 산식과 연도별 기준값이 준비된 범위에서만 계산하며, 국민연금공단 예상연금 조회와 차이가 날 수 있음을 고지한다. |
| 건강보험료 | 가입 유형(직장/지역), 보수월액 또는 소득월액, 지역가입자 재산점수, 기준연도 | 건강보험료, 장기요양보험료, 가입자 부담액 및 합계; 직장가입자는 사용자 부담 참고값을 분리 표시 | 직장가입자는 보수월액 기준, 지역가입자는 사용자가 입력한 재산점수의 간이 계산으로 제한한다. 자동차·경감·정산·피부양자 자격 등은 확정 판단 대상이 아니다. |
| 실업급여 | **기존 입력 유지:** 퇴직 전 월급, 연령, 고용보험 가입기간 | 1일 평균임금, 60% 기준액, 상·하한 반영 1일 지급액, 소정급여일수, 총 예상액 | 실제 근로일수, 이직사유, 수급자격, 재취업활동과 다수 피보험자 이력에 따라 결과가 달라짐을 결과 상단에 표시한다. 고용24도 모의계산만으로 수급자격이 인정되지 않는다고 안내한다.[5] |
| 최저임금 월급 | 기준연도, 시급(기본값은 정책 데이터), 1일 소정근로시간, 주 소정근로일수, 주휴 포함 여부, 월 환산시간 | 최저시급 충족 여부, 일급, 주 기본급, 주휴수당, 주급, 월 환산 최저임금 | 2026년 시급은 10,320원이며 1일 8시간 기준 일급은 82,560원이다.[6] 월 환산값은 주 40시간·유급주휴 등 명시된 가정에서만 표시하고, 적용 제외·감시단속·수습 및 계약별 예외는 별도 확인을 안내한다. |

### 4.4 사회보험 수치의 적용 기준

2026년 국민연금 사업장가입자 보험료는 사용자·근로자가 각각 4.75%를 부담하며, 기준소득월액 상·하한은 2026년 7월부터 2027년 6월까지 각각 659만원과 41만원이다.[7] 건강보험공단은 2026년 직장가입자 건강보험료율 7.19%와 장기요양보험료율 0.9448%를 안내하고 있다.[8] 따라서 연도 또는 적용기간을 무시한 단일 상수는 사용하지 말고, 사용자에게 해당 정책 버전을 보여 주어야 한다.

| 정책 키 예시 | 2026 적용값 | 적용 및 표시 규칙 |
| --- | --- | --- |
| `nps.workplace.employeeRate` | 4.75% | 사업장가입자 근로자 부담분으로 표시한다. |
| `nps.standardIncome.min/max` | 410,000원 / 6,590,000원 | 2026-07-01~2027-06-30 정책 버전에 한정한다. |
| `nhis.workplace.totalRate` | 7.19% | 가입자·사용자 각 50% 부담임을 표로 분리한다. |
| `nhis.longTermCareRate` | 0.9448% | 건강보험료와의 관계를 계산 과정에 표시한다. |
| `minimumWage.hourly` | 10,320원 | 2026-01-01 이후 적용값이며, 화면에 적용연도와 출처를 함께 보인다. |
| `unemployment.*` | 버전 데이터로 관리 | 일 상·하한 및 소정급여일수 표는 고용24·고용노동부 확인 후 해당 기준연도로 활성화한다. |

## 5. 5차 기능 요구사항: 날짜·시간·단위·일상생활

### 5.1 5차 카테고리·URL·탐색 설계

날짜 전용 도구는 새로운 `계산기 > 날짜·시간 계산기` 하위 카테고리(`slug: date`)에 추가한다. 이는 현재 URL 생성 규칙과 충돌하지 않으면서 `/calculator/date/*` 형식을 만든다. 근무시간·시급·단위·평수는 이미 서로 다른 기존 카테고리와 URL을 사용하므로 이동하지 않는다.

| 기능 | 구분 | 카테고리 배치 | 정규 URL | 핵심 검색 별칭 | 비고 |
| --- | --- | --- | --- | --- | --- |
| 날짜 계산기 | 신규 | 날짜·시간 계산기 > 날짜 연산 | `/calculator/date/date-calculator` | 날짜 계산, 날짜 더하기, 날짜 빼기 | 날짜 덧셈·뺄셈과 목표일까지 기간을 제공 |
| D-Day 계산기 | 신규 | 날짜·시간 계산기 > 날짜 연산 | `/calculator/date/d-day` | D-Day, 디데이, 기념일, 남은 날짜 | D-Day 표기 규칙을 고정 |
| 나이 계산기 | 신규 | 날짜·시간 계산기 > 나이 | `/calculator/date/age` | 나이 계산, 세는 나이, 연 나이 | 만 나이·세는 나이·연 나이를 함께 표시 |
| 만 나이 계산기 | 신규 | 날짜·시간 계산기 > 나이 | **슬러그 확정 필요** | 만나이, 만 나이, 국제나이 | 아래 URL 결정 항목을 선행한다. |
| 날짜 차이 계산기 | 신규 | 날짜·시간 계산기 > 날짜 연산 | `/calculator/date/date-difference` | 날짜 차이, 기간 계산, 며칠 차이 | 제외일/포함일을 명확히 구분 |
| 근무시간 계산기 | 기존 유지·보강 | 급여·직장인 > 근로·수당 유지 | `/calculator/salary/work-hours` | 근무시간, 출퇴근 시간, 휴게시간 | 자정 경과 계산을 유지 |
| 시간 계산기 | 신규 | 날짜·시간 계산기 > 시간 연산 | `/calculator/date/time-calculator` | 시간 계산, 시간 더하기, 시간 빼기 | 시:분 단위의 더하기·빼기 |
| 시간당 급여 계산기 | 기존 유지·보강 | 급여·직장인 > 근로·수당 유지 | `/calculator/salary/hourly-wage` | 시간당 급여, 시급, 월급 시급 | 기존 시급 계산기와 동일 도구 |
| 단위 변환 계산기 | 기존 유지·보강 | 단위 변환 유지 | `/units/unit-convert` | 단위 변환, 환산, 길이, 무게, 부피, 온도 | 부피 정의를 추가 |
| 평수 계산기 | 기존 유지·보강 | 부동산 계산기 유지 | `/calculator/real-estate/pyeong` | 평수 계산, 평, 제곱미터, ㎡ | 기존 양방향 변환 유지 |

**만 나이 URL 결정:** 제공된 예시는 `/calculator/date/age`와 `/calculator/date/age-korean`이나, 명칭상 `age-korean`은 “세는 나이”에 더 자연스럽다. 중복·오해를 막기 위해 다음 중 하나를 출시 전에 확정한다. 권장안은 **`/calculator/date/age` = 나이 비교 계산기(만 나이·세는 나이·연 나이), `/calculator/date/man-age` = 만 나이 전용 계산기**다. 예시 URL을 반드시 유지해야 한다면 `/age-korean`을 “세는 나이 계산기”로 명확히 표기하고 “만 나이 계산기”는 `/age` 결과 내 주요 모드로 통합해야 한다.

### 5.2 날짜·시간 계산의 공통 정확성 기준

날짜 기능은 시간대가 섞인 `Date` 객체의 밀리초 차이만으로 계산하면 일광절약시간제 등 환경에서 하루 오차가 생길 수 있다. 따라서 입력일은 `YYYY-MM-DD`의 **달력일**로 해석하고, 날짜 차이는 일 단위 정규화 함수로 계산한다. 월 단위 결과는 일수를 30으로 나눈 근사치가 아니라 “완전한 달 수 + 남은 일수”로 보여 준다.

| 공통 규칙 | 수용 기준 |
| --- | --- |
| 기준일 기본값 | 브라우저의 로컬 오늘 날짜를 기본값으로 사용하되, 사용자가 명시적으로 바꿀 수 있어야 한다. |
| 날짜 유효성 | 존재하지 않는 날짜, 빈 날짜, 종료일이 시작일보다 이른 조건을 즉시 설명형 오류로 처리한다. |
| D-Day 기준 | `목표일 - 기준일`의 달력일 차이를 쓴다. 목표일이 기준일이면 `D-Day`, 미래면 `D-n`, 과거면 `D+n`으로 표시한다. |
| 포함일 옵션 | 날짜 차이 기본값은 종료일−시작일(시작일 미포함)이며, “양 끝 날짜 포함” 옵션을 켜면 1일을 추가한다. 둘을 동시에 혼동되게 표시하지 않는다. |
| 윤년·월말 | 2월 29일, 28/29/30/31일의 월말 이월, 윤년 생일을 자동 테스트한다. |
| 시간 연산 | `HH:MM` 24시간 입력을 사용한다. 자정 경과 시 결과에 `다음 날` 또는 `전날`을 명시한다. |
| 접근성 | 날짜 입력은 키보드로 조작 가능하고 레이블·오류 메시지·결과 변경이 보조기술에 전달되어야 한다. |

### 5.3 5차 계산기별 상세 요구사항

| 계산기 | 최소 입력값 | 결과·계산 과정 | 경계 및 예외 |
| --- | --- | --- | --- |
| 날짜 계산기 | 기준일, 연산(더하기/빼기), 연·월·일 증감값 또는 목표일 | 결과 날짜, 요일, 기준일과의 일수 차이, 목표일까지 남은/지난 기간 | 월 단위 더하기는 월말 보정 규칙을 화면 도움말에 명시한다. |
| D-Day 계산기 | 기준일, 목표일, 이벤트명(선택) | D-Day 문자열, 남은/지난 총일수, 주·일 단위, 기준일·목표일 | 목표일이 과거여도 오류가 아니라 경과일로 표시한다. |
| 나이 계산기 | 생년월일, 기준일 | 만 나이, 세는 나이, 연 나이, 다음 생일까지 남은 일수 | “세는 나이”는 관습상 비교 정보이며 법적 판단 기준은 아니라고 표시한다. |
| 만 나이 계산기 | 생년월일, 기준일 | 만 나이, 올해 생일 경과 여부, 다음 생일 | 윤년 2월 29일 출생자의 비윤년 기준일 처리 규칙을 FAQ에 명시하고 테스트한다. |
| 날짜 차이 계산기 | 시작일, 종료일, 포함일 옵션 | 총 일수, 포함 일수, 완전한 주 수+남은 일수, 완전한 달 수+남은 일수 | 종료일이 앞서는 경우 계산을 막고 날짜 교체 안내를 표시한다. |
| 근무시간 계산기 | **기존 입력 유지:** 출근·퇴근 시각, 휴게시간 | 총 경과시간, 휴게시간, 실제 근무시간, 자정 경과 여부 | 휴게시간이 전체시간보다 길면 결과 대신 오류를 표시한다. |
| 시간 계산기 | 기준 시각, 연산, 시간·분 증감값 | 결과 시각, 전날/당일/다음날 표시, 총 증감분 | 음수 증감은 연산 선택으로 정규화하고, 24시간 초과를 정상 처리한다. |
| 시간당 급여 계산기 | **기존 입력 유지:** 월급 또는 시급, 월 환산시간 | 시급, 월급, 주급 추정 | 시급·최저임금 비교는 정책 버전이 선택된 경우에만 부가 표시한다. |
| 단위 변환 계산기 | 카테고리, 입력값, 변환 전·후 단위 | 변환값, 사용한 환산 기준, 역변환 값 | 길이·무게·넓이·부피·온도를 제공하고, 온도는 별도 선형 변환을 적용한다. |
| 평수 계산기 | 면적값, 입력 단위(㎡/평) | ㎡, 평, 표시용 반올림값, 환산식 | 내부 계산은 `1평 = 3.305785㎡`를 유지하며 표시 반올림은 계산값과 분리한다. |

### 5.4 단위 변환 보강 범위

기존 변환 도구는 길이·무게·넓이·온도를 지원하므로 유지한다. 5차 요구사항 충족을 위해 **부피** 단위 정의와 선택 UI를 보강한다. 온도는 오프셋이 있는 단위이므로 다른 단위처럼 단순 계수 곱셈을 적용해서는 안 된다.

| 그룹 | 최소 단위 | 기준 단위 | 계산 규칙 |
| --- | --- | --- | --- |
| 길이 | mm, cm, m, km, in, ft | m | 계수 기반 변환 |
| 무게 | g, kg, t, lb, oz | kg | 계수 기반 변환 |
| 넓이 | ㎡, 평, ft², ha | ㎡ | 계수 기반 변환; 평은 3.305785㎡ |
| 부피 | mL, L, m³, cup, gal | L 또는 m³ | 단위 정의의 기준 국가·종류(미국 갤런 등)를 라벨로 명시 |
| 온도 | ℃, ℉, K | ℃ | 섭씨 중간값을 거치는 오프셋 변환 |

## 6. 공통 UI, 콘텐츠, 검색 및 SEO 요구사항

모든 신규 계산기는 기존 공통 프레임을 사용하고, 외형을 새로 설계하지 않는다. 결과 영역은 입력 이전에는 계산 방법을 안내하고, 계산 이후에는 핵심 결과와 상세 과정을 구분해 표시한다. 세금·사회보험 결과의 안내문은 결과보다 눈에 띄지 않게 숨기지 말고, 같은 화면에서 확인 가능해야 한다.

| 영역 | 필수 요구사항 |
| --- | --- |
| 상단 | 브레드크럼, H1, 1~2문장 소개, 해당 정책 기준일 또는 계산 기준 표시 |
| 입력 | 입력 레이블, 단위, 예시 값, 필수 여부, 숫자 키패드 유도, 날짜/시간 제어, 인라인 오류 |
| 액션 | 기존의 **계산하기** 및 **초기화** 컴포넌트를 공통으로 사용한다. Enter 동작과 초기화 후 기본값 복원을 확인한다. |
| 결과 | 큰 핵심 결과, 항목별 계산 과정, 사용한 가정, 반올림 기준, 범위 제외·주의 안내를 제공한다. |
| 지식 콘텐츠 | 계산 공식, 계산 방법, 계산 예시, 주의사항, FAQ, 관련 계산기 섹션을 기존 `ToolKnowledge` 구조로 제공한다. |
| 관련 도구 | `relatedToolIds`에는 실제 활성 도구만 넣는다. 링크가 없는 임의 경로를 만들지 않는다. |
| 검색 | 각 도구의 `searchKeywords`에 정식명·약칭·띄어쓰기 변형을 넣는다. 검색 결과에는 도구명·상위/하위 카테고리·설명·정규 URL을 표시한다. |
| 인기 도구 | 기존 `isPopular`, `sortOrder` 구조를 사용한다. 기존 인기 도구는 삭제하지 않고, 4차 후보는 연말정산·최저임금·건강보험료, 5차 후보는 D-Day·날짜 계산기로 제안한다. 홈페이지에는 최대 9개 규칙을 지킨다. |
| SEO | 도구별 고유 title·description, canonical, `WebApplication` 구조화 데이터, 실제 본문 콘텐츠를 제공한다. 카테고리에는 `CollectionPage`를 유지한다. |

## 7. 데이터·아키텍처·운영 요구사항

정책 숫자가 변경될 때 코드 전체를 수정하지 않도록, 계산 공식과 정책 데이터의 책임을 분리한다. 세법·사회보험과 최저임금은 변경 주기가 다르므로, “2026”이라는 단일 상수 묶음만으로 운영해서는 안 된다. 기존 카탈로그와 별개로 `calculationPolicy` 데이터 계층을 도입하거나, 최소한 동일 구조의 버전 JSON을 공유 모듈로 두어야 한다.

| 구성요소 | 요구사항 | 예시 |
| --- | --- | --- |
| 정책 세트 | 코드, 버전, 유효 시작/종료, 검증일, 공식 출처, 변경 메모를 보관한다. | `tax-income-2026`, `nhis-2026`, `nps-2026-07`, `minimum-wage-2026` |
| 정책 값 | 수치, 단위, 반올림 규칙, 적용 조건, 범위 제한을 구조화한다. | 세율 구간 배열, 공제 한도, 상·하한, 보험료율 |
| 계산 함수 | DOM·상태에 의존하지 않는 순수 함수로 작성하고, 결과에 `assumptions`, `warnings`, `policyVersion`을 포함한다. | `calculateGiftTax(inputs, policy)` |
| 화면 | 입력을 검증·정규화한 뒤 계산 함수만 호출한다. 실패 시 오류를 표시하며 계산 결과를 조작하지 않는다. | 전용 페이지 또는 같은 도메인의 계산기 묶음 컴포넌트 |
| 카탈로그 | 각 도구에 `logicKey`, `formula`, FAQ, SEO, 별칭, 실제 관련 도구 ID를 입력한다. | 슬러그 등록 후 안전 라우터 연결 |
| 관리자 운영 | 출처 URL, 기준일, 정책 버전, 공개 상태, 인기 여부·검색 별칭을 수정할 수 있도록 한다. 정책 수치를 관리자 편집으로 열 경우 변경 이력·검토자·되돌리기를 남긴다. | 정책 변경 로그 |

## 8. 테스트 및 수용 기준

### 8.1 전 계산기 공통 수용 기준

신규 14개 도구와 보강 대상 6개 도구 모두 아래 테스트 매트릭스를 통과해야 한다. 세금·보험·연금은 단위 테스트만으로 충분하지 않으므로, 공식 기관 예시 또는 검토 승인된 기준표의 샘플과 대조하는 회귀 테스트를 포함한다.

| 테스트 유형 | 필수 케이스 | 통과 조건 |
| --- | --- | --- |
| 정상 입력 | 대표적인 현실 입력값 | 핵심 결과·세부 과정·단위·반올림이 정책/공식과 일치한다. |
| 빈 입력 | 필수값 전체/일부 미입력 | 계산하지 않고 각 입력의 해결 가능한 오류를 표시한다. |
| 0 입력 | 금액·기간·시간·공제값 0 | 허용되는 0은 계산하고, 불가능한 0은 이유를 안내한다. |
| 음수 입력 | 금액·시간·일수 음수 | 정의되지 않은 입력은 차단·설명하고, 시간 빼기처럼 유효한 연산은 명시적 모드로만 허용한다. |
| 소수 입력 | 금액, 이율, 시간(분), 환산값 | 허용 자릿수와 반올림이 일관되며 NaN/Infinity가 노출되지 않는다. |
| 큰 수 | 고액 재산·장기 기간·대형 단위 | UI가 깨지지 않고 금액 서식·오버플로 없이 결과를 보여 준다. |
| 잘못된 형식 | 문자, 유효하지 않은 날짜, 25:61 시각 | 앱 오류 없이 인라인 오류를 표시하고 마지막 정상 결과를 확정값처럼 남기지 않는다. |
| 계산·초기화 | 기본값, 수정 후 계산, 초기화 | 버튼·키보드 동작이 정상이며 초기화가 정책 기본값·빈값 상태로 복원된다. |
| 모바일 | 390×844 및 360×800 | 입력, 결과표, FAQ, 관련 링크에서 가로 스크롤·겹침·잘림이 없다. |
| 접근성 | 키보드, 레이블, 오류·결과 알림 | 마우스 없이 계산 가능하고 폼 연결 관계가 유지된다. |

### 8.2 도메인별 필수 회귀 케이스

| 도메인 | 추가 필수 테스트 |
| --- | --- |
| 종합소득·상속·증여세 | 모든 누진세율 경계값의 직전/정확히/직후, 공제 상한, 이전 증여 합산, 지방소득세 계산, 정책 유효기간 밖 연도 |
| 양도소득세 | 1년 미만·1년 이상 2년 미만·2년 이상 보유, 취득/양도일 역전, 필요경비가 양도차익보다 큰 경우, 범위 제외 자산 |
| 연말정산·부가세 | 기납부세액보다 결정세액이 작은/큰 경우, 비과세가 총급여를 초과하는 경우, 매입세액이 매출세액보다 큰 경우 |
| 연금·보험·최저임금·실업급여 | 상·하한, 적용일 경계, 가입유형별 결과, 자격 미충족 경고, 최저임금 월 환산 가정, 출처 표시 |
| 날짜·시간 | 윤년 2월 29일, 월말 더하기/빼기, 같은 날짜, 시작일>종료일, 자정 경과, 24시간 이상 증감, 포함일 옵션 |
| 단위·평수 | 양방향 왕복 오차, 온도 동치점, 부피 단위 정의, 매우 큰/작은 값, 표시 자릿수 |
| 기존 기능 | 1~3차 대표 계산기, 모든 기존 URL, 기존 검색 별칭, 홈페이지 인기 도구, 법적 안내 페이지 |

### 8.3 출시 게이트

4차를 먼저 배포 가능한 상태로 완결한 뒤 5차를 진행한다. 각 차수는 아래 게이트를 모두 통과해야 다음 차수로 넘어간다.

| 게이트 | 4차 통과 조건 | 5차 통과 조건 |
| --- | --- | --- |
| 기능 | 신규 8개와 기존 2개 보강 항목이 카탈로그·라우팅·실계산으로 연결됨 | 신규 6개와 기존 4개 보강 항목이 카탈로그·라우팅·실계산으로 연결됨 |
| 기준 검토 | 정책 버전·출처·적용일·면책 범위가 세금/보험 화면에 표시됨 | 날짜·시간의 포함일·시간대·윤년 규칙과 단위 기준이 표시됨 |
| 품질 | 단위 테스트, 타입 검사, 프로덕션 빌드, 신규 URL 200, 검색, 모바일·콘솔 검증 통과 | 동일 |
| 회귀 | 1~3차의 대표 URL·계산·검색·홈 인기 도구에 회귀 오류 없음 | 동일 |
| 운영 | 관리자에게 정책 업데이트 책임자·검증 주기·롤백 절차가 전달됨 | 신규 날짜 카테고리·검색 별칭·인기 노출 설정이 확인됨 |

## 9. 권장 구현 순서

첫 단계에서는 정책 데이터 모델과 날짜 공통 함수를 먼저 확정한다. 이후 4차는 세금 중 정책 의존성이 낮은 부가세 보강·최저임금부터, 그다음 종합소득·증여·상속·양도·연말정산, 마지막으로 국민연금·건강보험·실업급여의 기준 검증 순으로 진행한다. 단, 국민연금 예상수령액은 공인 산식 및 연도별 기준값을 검증하기 전에는 “예상 수령액”으로 공개하지 않는다.

5차는 날짜 유틸리티를 만든 뒤 날짜 계산기·날짜 차이·D-Day·나이·만 나이·시간 계산기의 공통 회귀 테스트를 먼저 작성한다. 근무시간·시급·단위·평수는 기존 도구의 핵심 로직을 건드리지 않고, 검색 별칭·관련 링크·부피 정의·모바일 검증만 독립 커밋으로 보강한다.

| 순서 | 산출물 | 완료 판단 |
| --- | --- | --- |
| 1 | 정책 데이터 구조·출처 레지스트리·테스트 표본 | 수치가 화면 코드에 직접 흩어져 있지 않고, 버전·출처·유효기간을 조회할 수 있음 |
| 2 | 4차 세금/사회보험 계산 함수와 페이지 | 10개 사용자 접근 항목이 실제 계산·설명·검색·관련 링크로 동작함 |
| 3 | 4차 품질 보고 | 정책 확인, 자동 테스트, 타입·빌드, URL, 모바일, 회귀 결과가 기록됨 |
| 4 | 날짜/시간 공통 유틸리티와 5차 신규 페이지 | 달력일 규칙·경계값 테스트를 포함한 6개 신규 계산기가 동작함 |
| 5 | 기존 5차 대상 4개 보강 및 최종 회귀 | 기존 URL/로직을 보존하면서 검색·단위·모바일 요구사항을 충족함 |
| 6 | 최종 운영 보고 | 4차·5차 목록, URL, 입력값, 공식, 테스트, 검색, 내부 링크, 홈페이지, 오류 여부를 분리 보고함 |

## 10. 출시 전 확정이 필요한 의사결정

요구사항을 구현 가능한 수준으로 구체화했지만, 아래 세 항목은 계산 결과의 법적 오해를 막기 위해 출시 전에 운영자가 결정해야 한다. 이 결정을 미루면 해당 기능은 **초안/비공개 상태**로 두어야 한다.

| 의사결정 | 권장안 | 미결 시 위험 |
| --- | --- | --- |
| 국민연금 예상수령액의 공식 산식 검증 | 국민연금공단 기준 산식·연도별 기준값·수급개시 연령을 법무/정책 검토 후 버전 데이터로 등록한다. | 단순 추정치가 실제 공적연금 예상액처럼 오인될 수 있다. |
| 양도소득세의 1차 지원 범위 | “일반 부동산, 특정 보유기간, 비과세·다주택·특례 제외”로 출발하고, 복잡 조건은 버전별 모드로 확장한다. | 전부 지원한다고 보이면 1주택 비과세·중과·특례에서 큰 오차가 발생한다. |
| 나이 계산기 URL·정보 구조 | `/age`를 만 나이·세는 나이 비교 페이지로, 만 나이 전용은 명확한 별도 slug로 정한다. | 예시의 `age-korean`과 ‘만 나이’ 명칭이 충돌해 SEO·검색·사용자 기대가 엇갈린다. |
| 정책 데이터 수정 권한 | 수치 변경은 관리자 입력만으로 즉시 반영하지 말고, 출처·검토자·승인·롤백을 남기는 검토 흐름을 둔다. | 잘못된 세율·보험료가 즉시 공개될 수 있다. |

## 11. 참고 자료

[1]: https://www.nts.go.kr/nts/cm/cntnts/cntntsView.do?mi=6529&cntntsId=7957 "국세청 — 상속세 세율"

[2]: https://www.nts.go.kr/nts/cm/cntnts/cntntsView.do?mi=6533&cntntsId=7960 "국세청 — 증여세 항목별 설명"

[3]: https://www.nts.go.kr/nts/cm/cntnts/cntntsView.do?mi=2312&cntntsId=7711 "국세청 — 양도소득세 세율"

[4]: https://www.nts.go.kr/nts/cm/cntnts/cntntsView.do?mi=2272&cntntsId=7706 "국세청 — 연말정산 예상세액 계산 안내"

[5]: https://m.work24.go.kr/cm/c/f/1100/selecSimulate.do?systId=SI00000001 "고용24 — 구직급여 지급액 모의계산"

[6]: https://www.moel.go.kr/info/lawinfo/instruction/view.do?bbs_seq=20250800121 "고용노동부 — 2026년 적용 최저임금 고시"

[7]: https://www.nps.or.kr/eng/ntnlpnsplan/cntb/getOHAI0013M0.do "국민연금공단 — 보험료 및 2026년 기준소득월액 상·하한"

[8]: https://edi.nhis.or.kr/portal/images/popup/20251204_pop01longdesc.html "국민건강보험공단 — 2026년도 보험료율 안내"

---

> **최종 원칙:** 4·5차 확장은 기존 도구상자를 대체하는 프로젝트가 아니라, 검증 가능한 계산기와 운영 가능한 정책 데이터를 현재의 카탈로그 구조에 추가하는 프로젝트다. 정책 확인이 필요한 항목은 기능을 서둘러 확정값처럼 보여 주기보다, 적용 범위·출처·기준일을 명확히 하여 신뢰도를 우선한다.
